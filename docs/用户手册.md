# 四川物流工单系统 — 用户手册

> 面向使用者：部门内登记工单、跟进需求的同事。
> 系统无需注册登录，打开浏览器访问地址即可使用。

---

## 一、系统是做什么的

一句话：**把部门里的工作工单登记下来，再把需要开发的需求从工单转化出来，一路跟到发版。**

两条主线：

```
工单（WorkOrder）：日常工作的登记与追踪
  └─ 需要开发的，一键「转需求」→

需求（Requirement）：跟方案 → 开发 → 测试 → 合并 → 发布
```

页面只有三个入口：**首页**（导航）、**工单管理**、**需求管理**。

---

## 二、访问系统

在浏览器打开管理员提供的地址，例如 `http://192.168.1.100:3000`。

推荐使用 Chrome / Edge 最新版。系统适配了窄屏，但表格列较多，**建议在 1440 宽以上窗口使用**，体验最佳。

---

## 三、工单管理

### 3.1 查看工单列表

顶部导航点击「工单管理」。

列表列含义见 [5.1 工单字段说明表](#51-工单字段说明表-workorder)。

### 3.2 新建工单

1. 点击左上角 **「新建工单」**；
2. 填写表单（带 <span style="color:red">*</span> 为必填）：

| 字段 | 是否必填 | 默认值 | 说明 |
|------|---------|--------|------|
| 日期 | ✅ | **自动填今天** | 日期选择器 |
| 标题 | ✅ | 空 | 一句话概括 |
| 工单内容 | ✅ | 空 | 详细描述 |
| 系统 | ✅ | WMS | 下拉：WMS / ERP / OMS / TMS / 其他 |
| 状态 | ✅ | 新建 | 下拉：新建 / 已处理 / 已关闭 |
| 是否转需求 | ❌ | 否 | 一般不用手改，转需求时会自动置为「是」 |
| 备注 | ❌ | 空 | 补充信息 |

3. 点击 **「提交」** 保存；点「取消」放弃返回列表。

> 保存成功后会回到列表并出现「创建成功」提示。

### 3.3 编辑工单

两种方式：

- **整条编辑**：列表操作列点「编辑」，改完点「提交」；
- **列表内快捷修改**：直接在列表里改「系统 / 是否转需求 / 状态」这三个字段 —— 这类修改**不会立即保存**，会先变成待提交状态（单元格上有标记，左侧「提交」按钮出现数字，如「提交（2）」表示 2 条待提交）。

> ⚠️ **这是本系统的核心规则：改完必须点「提交」并确认，才会写入数据库。**
> 忘记点提交就切换页面，修改会丢失。
> 点「还原」可一次性放弃所有未提交的改动。

### 3.4 搜索与筛选

- **按标题搜索**：右上角输入框，实时过滤（不区分大小写）；
- **按系统筛选**：右上角下拉框，可清空；
- 两者可叠加使用。

### 3.5 转需求（最常用的功能）

当一条工单确认需要开发时：

1. 在该行操作列点击 **「转需求」**；
2. 弹窗确认后，系统自动完成：

   - 按 `R-年份-序号` 规则**自动生成需求编号**（如 `R-2026-004`，自动跳过已占用编号）；
   - 把工单的**日期、标题、内容、系统**同步到需求；
   - 需求的初始节点设为 **方案中**；
   - 工单的「是否转需求」置为 **是**，且该按钮变为禁用，防止重复转换。

3. 之后在「需求管理」里就能看到这条需求，工单列表的「关联需求」列会显示对应的需求编号。

> 一条工单最多转出一条需求（一对一）。若提示「该工单已转需求」，说明此前已转换过。

### 3.6 删除工单

操作列点「删除」并确认。

> ⚠️ **已转为需求的工单不能直接删除**。系统会提示「该工单已转为需求（R-…），请先删除关联需求再删除工单。」
> 这是为了保护数据一致性：需求还在跟进，来源工单不该凭空消失。

### 3.7 自定义列的显示顺序与宽度

- **拖动列标题**可调整列顺序；**拖动列边界**可调整列宽；
- 系统会**记住你的列布局**，下次打开仍然生效（保存在本机浏览器中，换电脑不通用）；
- 想恢复原样，点击表格上方的 **「还原默认列布局」**。

### 3.8 刷新数据

列表数据有缓存，页面来回切换**不会重复请求**。
需要看最新数据时，点右上角 **「刷新」**。

---

## 四、需求管理

### 4.1 查看需求列表

顶部导航点击「需求管理」。需求的来源有两种：**工单转来的**（显示来源工单标签）和**手工新建的**。

### 4.2 手工新建需求

点击「新建需求」，填写表单：

| 字段 | 是否必填 | 默认值 | 说明 |
|------|---------|--------|------|
| 日期 | ✅ | **自动填今天** | 需求提出日期 |
| 需求ID | ✅ | 空 | **全局唯一**，如 `R-2026-010`；重复会提示「编号已存在」 |
| 需求标题 | ❌ | 空 | |
| 需求内容 | ❌ | 空 | |
| 系统 | ✅ | WMS | 下拉选择 |
| 当前节点 | ✅ | 方案中 | 下拉：方案中 / 开发中 / 测试中 / 已合并 / 已发布 |
| 是否加急 | ✅ | 否 | 加急需求在列表中显示红色标签 |
| 是否发版 | ✅ | 否 | 选「是」时下方出现 **发版时间**（必填，默认今天） |
| 备注 | ❌ | 空 | |
| 开发时长 | 自动 | — | **只读**，见 4.5 |

### 4.3 编辑需求

- **整条编辑**：操作列点「编辑」；
- **列表内快捷修改**：「系统 / 当前节点 / 是否加急 / 是否发版」可直接改，同样需要点 **「提交」** 才保存。

关于「是否发版」的提醒：
- 从 **否 → 是**：系统自动把**发版时间填为今天**（可再去编辑页改成实际发版日）；
- 从 **是 → 否**：**发版时间会被清空**，开发时长改回按「今天」计算。

### 4.4 搜索与筛选

- **按需求ID/标题搜索**：右上角输入框；
- **按当前节点筛选**：右上角下拉框，例如只看「测试中」。

### 4.5 开发时长怎么算的

**自动计算，不可手工填写**（手工填的值会被系统忽略）：

| 情形 | 计算方式 |
|------|---------|
| 未发版 | 需求日期 → **今天** |
| 已发版 | 需求日期 → **发版时间** |

两端**含当天**，结果为 0 时显示 0（不会出现负数）。

> 例：日期 2026-08-01，今天 2026-09-10，未发版 → 开发时长 40 天。

### 4.6 删除需求

操作列点「删除」并确认。若该需求来自工单，删除后**来源工单的「是否转需求」会自动复位为「否」**，该工单便可重新转需求或直接删除。

---

## 五、字段说明表

### 5.1 工单字段说明表（WorkOrder）

| 字段 | 类型 | 必填 | 默认 | 列表显示 | 说明 |
|------|------|:----:|------|---------|------|
| 工单ID | 系统生成 | — | 自动 | `wo-` + 8 位 | 系统内部标识，不可编辑 |
| 日期 | 日期 | ✅ | 今天 | ✅ | 工单登记日期 |
| 标题 | 文本 | ✅ | — | ✅ | |
| 工单内容 | 多行文本 | ✅ | — | ✅ | 超长省略，编辑页可看全文 |
| 系统 | 下拉 | ✅ | WMS | ✅ 可改 | WMS / ERP / OMS / TMS / 其他 |
| 是否转需求 | 布尔 | ❌ | 否 | ✅ 可改 | 转需求后自动置「是」 |
| 关联需求 | 系统派生 | — | — | ✅ | **自动反查**：该工单转出的需求编号；未转需求显示 `-` |
| 状态 | 下拉 | ✅ | 新建 | ✅ 可改 | 新建 / 已处理 / 已关闭 |
| 备注 | 文本 | ❌ | 空 | ✅ | |
| 创建时间 | 系统 | — | 自动 | — | 记录创建时刻 |
| 更新时间 | 系统 | — | 自动 | — | 每次保存刷新 |

> 说明：设计文档中工单侧的「关联的需求ID / 需求内容」两项信息，实现上统一由需求表反向关联（需求持有 `workOrderId`），列表「关联需求」列即为该信息的展示，效果一致且避免两处重复存储导致不一致。

### 5.2 需求字段说明表（Requirement）

| 字段 | 类型 | 必填 | 默认 | 列表显示 | 说明 |
|------|------|:----:|------|---------|------|
| 需求ID | 文本 | ✅ | — | ✅ | **全局唯一**，格式建议 `R-年份-序号`；工单转需求时自动生成 |
| 日期 | 日期 | ✅ | 今天 | ✅ | 需求提出日期 |
| 需求标题 | 文本 | ❌ | 空 | ✅ | 工单转需求时同步自工单标题 |
| 需求内容 | 多行文本 | ❌ | 空 | ✅ | 工单转需求时同步自工单内容 |
| 系统 | 下拉 | ✅ | WMS | ✅ 可改 | WMS / ERP / OMS / TMS / 其他 |
| 开发时长(天) | 系统计算 | — | 自动 | ✅ 只读 | 未发版：日期→今天；已发版：日期→发版时间（含当天，下限 0） |
| 当前节点 | 下拉 | ✅ | 方案中 | ✅ 可改 | 方案中 / 开发中 / 测试中 / 已合并 / 已发布 |
| 是否加急 | 下拉 | ✅ | 否 | ✅ 可改 | 选「是」列表显示红色标签 |
| 是否发版 | 布尔 | ✅ | 否 | ✅ 可改 | 置「是」自动填发版时间=今天 |
| 发版时间 | 日期 | 条件必填 | — | ✅ | 仅「是否发版=是」时必填并显示 |
| 来源工单 | 系统派生 | — | — | ✅ | 工单转需求时写入；手工新建的需求显示 `-` |
| 备注 | 文本 | ❌ | 空 | ✅ | |
| 创建时间 | 系统 | — | 自动 | — | |
| 更新时间 | 系统 | — | 自动 | — | |

---

## 六、操作速查（常见问题）

**Q：我改了列表里的下拉框，切到别的页面回来发现没变？**
内联修改必须点左侧 **「提交」** 按钮并在弹窗中确认才会保存。这是系统的防误操作设计。

**Q：提交时提示「请先处理…」或红色错误？**
错误提示会写明原因，例如编号重复、工单已转需求。按提示调整后重试即可。

**Q：删不掉工单，提示要先删除关联需求？**
先到「需求管理」找到对应需求（列表「来源工单」列可识别）删除，再删工单。

**Q：需求开发时长看起来不对？**
检查「日期」「是否发版」「发版时间」三项。时长由它们自动推导，不能手工改。

**Q：列表列的顺序乱了 / 想恢复原样？**
点表格上方的「还原默认列布局」。

**Q：数据是别人刚录的，我这边没看到？**
点右上角「刷新」。

**Q：误删了数据能恢复吗？**
删除不可撤销，需联系管理员从数据库备份恢复。请谨慎操作。

**Q：能两个人同时改同一条记录吗？**
可以，但**后提交的会覆盖先提交的**。建议同一条记录由同一人维护。

---

## 七、给管理员的话

- 使用过程中发现的 Bug 或改进建议，请一并收集后反馈（对应设计文档中的环节9「上线后维护与迭代」）。
- 数据备份：SQLite 场景备份 `prisma/dev.db`；PostgreSQL 场景用 `pg_dump`。
- 部署、升级、排障详见 [`docs/部署指南.md`](./部署指南.md)。
