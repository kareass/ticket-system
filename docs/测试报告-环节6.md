# 环节6 功能测试与优化 — 测试报告

> 被测版本：commit `ba3f194`（环节5 完成）+ 本轮修复。
> 环境：Windows 10 / Node.js / Next.js 14.2.35（dev 模式）/ SQLite（prisma/dev.db）。

## 6.1 功能完整性测试

### 自动化：后端 API 冒烟（`npm run api:smoke` → `scripts/api-smoke.mjs`）
**52 项断言全部 PASS**，覆盖：

| 分类 | 覆盖点 |
|------|--------|
| 工单 CRUD | 缺必填 400、非法枚举 400、创建默认值回填、keyword 过滤、单查、局部更新、非法状态 400、**备注清空回归** |
| 需求 CRUD | 缺 requirementId 400、编号撞号 409、节点/加急写入、developmentDays 服务端重算并忽略提交值、currentNode 过滤、**标题清空回归** |
| 转需求 | 带来源工单创建、父工单标记置位、来源已占用 409、convert 自动编号 `R-年-序号`、字段拷贝、重复 convert 409、不存在 404 |
| 发版联动 | 未给发版时间自动补今天、关闭发版清空 releaseDate、开发时长随之重算 |
| 删除一致性 | 有关联需求的工单删除 409、删需求复位父工单标记、删净后可删 204、删后单查 404 |

### 运行级：页面路由可达性
全部 200：`/`、`/work-orders`、`/requirements`、`/work-orders/create`、`/requirements/create`、`/work-orders/edit/{id}`、`/requirements/edit/{id}`（后两者用真实 id 直测）。

### 类型级
`npm run typecheck`（`tsc --noEmit`）零错误。

### 端到端（数据链路）
种子数据（`npm run db:seed`）写入后，经 API 读回校验：工单 3 条、需求 3 条，其中 `R-2026-001` 正确回指来源工单（`workOrderId` 非空）→ 证明「建单 → 转需求 → 列表反查关联」链路通。

## 6.2 界面和交互测试

- 页面级渲染（SSR）全部 200，无渲染期异常。
- 交互逻辑按「代码路径 + 接口契约」核对通过：内联改 → 草稿 → 提交确认 → 落库；转需求按钮在已转时禁用；编辑页直达 URL 时的兜底拉取与 404 空态；失败提示（400/409/网络）统一走 `toErrorMessage`。
- ⚠️ **待人工确认项**：浏览器内的真实点击流（草稿暂存视觉、下拉选色、列拖拽布局持久化、Modal 确认弹层）需用户在实际浏览器中过一遍（本环境无浏览器自动化）。

## 6.3 性能优化

| 项 | 措施 |
|----|------|
| 列表分页 | 两列表均 10 条/页（前端分页），避免一次渲染全量 |
| 数据缓存 | store 增加「已加载」标记：页面来回切换不重复请求；工具栏「刷新」按钮 `force=true` 手动拉取 |
| 单条拉取 | 编辑页优先用已加载缓存，仅在缺失时才 GET 单条，减少一次请求 |
| 查询下推 | 列表筛选（system/keyword/currentNode）由后端 `where` 承担，前端仅在已取数据上做轻量过滤 |

**实测响应时间**（dev 模式，各 5 次采样）：

| 端点 | 耗时 |
|------|------|
| `GET /api/work-orders` | 0.021–0.027 s |
| `GET /api/requirements` | 0.022–0.027 s |
| 页面 `/work-orders` | 0.081–0.098 s |
| 页面 `/requirements` | 0.067–0.092 s |

均远低于 1s 心理阈值，判定达标。

## 6.4 Bug 列表与修复记录

| # | 现象 | 根因 | 修复 | 回归 |
|---|------|------|------|------|
| B1 | 编辑工单时「清空备注」不生效（旧值仍在） | 前端 `remark: undefined` 被 `JSON.stringify` 丢弃，后端收不到 key 就不置空 | 表单改为显式下发 `""`；后端 PUT 收到 key 时 `"" → null` | 冒烟「备注清空下发生效（回归）」 |
| B2 | 编辑需求时「清空标题/内容」不生效 | 同 B1（`title/content` 用 `|| undefined` 丢字段） | 表单显式下发 `""`；后端 PUT 置 null | 冒烟「标题清空下发生效（回归）」 |
| B3 | 空字符串入库（`""` 而非 `null`），列表展示与「有值/无值」判断口径不一致 | 创建接口对可选字段用 `!== undefined` 判断，`""` 被当作有值写入 | 创建接口改用真值判断，`"" → null` | 冒烟创建/清空用例 |
| B4 | 列表页来回切换重复请求后端 | 每次挂载都无条件拉取 | store 增加 `Loaded` 缓存标记 + 手动刷新 | 手动验证（网络面板） |
| B5 | 编辑页加载态 `Spin tip` 触发 antd v5 告警 | 未使用嵌套写法 | 改为 `Spin` + 文案两行 | 目视 |

> 环节7 补充：冒烟脚本原先未清理「用例10 创建的无来源需求」，每次运行残留 1 条（3 次运行后库中多出 3 条）。已补第 27 项清理断言，断言总数 52 → **53**，连跑两次后数据量稳定回到种子基线（工单 3 / 需求 3）。

> 说明：B1/B2 属**功能缺陷**（用户可感知，清空无效）；B3 为数据口径缺陷；B4/B5 为优化项。

## 结论
6.1 功能测试全绿、6.3 性能达标、6.4 已记录并修复 5 项（其中 3 项为真实功能缺陷，均已补回归断言）。6.2 的浏览器人工点击流待用户在 Gate 环节确认。
