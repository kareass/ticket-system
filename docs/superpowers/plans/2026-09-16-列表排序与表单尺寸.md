# 列表双击排序 + 表单内容框尺寸 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给需求列表与工单列表加上「双击列名排序」，并把表单里的内容框/备注框加宽加高。

**Architecture:** 不使用 antd 内置 `sorter`（手势固定为单击、无法改成双击，且 ProTable 会覆盖 `onHeaderCell`、右缘拖宽手柄会遮挡排序箭头），改为自研：一个纯函数比较器模块 + 一个双击列头组件，排序状态存在页面级 `useState`，在 `useMemo` 里作用于已提交值。表单宽度由外层 `maxWidth` 容器决定而非 TextArea 自身，故改容器。

**Tech Stack:** Next.js 14 App Router / React 18 / TypeScript 5 / antd 5 / 自研 ProTable（`src/components/common/ProTable.tsx`）

**设计依据：** `docs/superpowers/specs/2026-09-16-列表排序与表单尺寸-design.md`

---

## 项目前提（实施者必读）

- **本项目没有单元测试框架**（无 jest/vitest）。`npm run api:smoke` 是后端接口冒烟，本次改动不涉及接口，**不要跑它**（它需要服务在 3000 端口运行，且会读写数据库）。
- 本次的自动化验证手段是两个：**`npm run typecheck`** 和新增的 **`npm run check:sort`**（纯函数断言，不碰数据库、不碰网络）。
- **UI 交互无法自动验证**，项目硬规则第 3 条规定必须由用户在浏览器中实测。Task 7 是交付门槛。
- Windows 上 dev server 运行时会锁住 `.next` 导致 `npm run build` 报 EPERM。本计划**不需要 build**，只跑 typecheck。

---

## 文件结构

**新建：**

| 文件 | 职责 |
|------|------|
| `src/lib/sortRows.ts` | 纯函数。排序比较器工厂，处理数值/布尔/字符串/枚举四类字段与空值语义。不依赖 React。 |
| `src/components/common/SortableHeader.tsx` | 纯展示组件。双击列头 + 排序指示符，通过 props 受控。 |
| `scripts/sort-check.ts` | 比较器的自动断言脚本，替代单元测试。 |

**修改：**

| 文件 | 改动 |
|------|------|
| `package.json` | 新增 `check:sort` 脚本 |
| `src/app/requirements/page.tsx` | 排序状态 + 6 个列头 |
| `src/app/work-orders/page.tsx` | 排序状态 + 3 个列头 + 转需求弹框 TextArea |
| `src/components/WorkOrderForm.tsx` | 容器宽度 + 3 处 rows |
| `src/components/RequirementForm.tsx` | 容器宽度 + 2 处 rows |
| `docs/四川物流工单系统-设计方案.md` | 功能范围回写 |
| `docs/用户手册.md` | 排序操作说明 |

**分工边界：** `sortRows.ts` 只管「怎么比」，不知道 React；`SortableHeader` 只管「怎么点」，不知道比什么；页面负责把两者接起来并持有状态。

---

## Task 1: 排序比较器 `src/lib/sortRows.ts`

**Files:**
- Create: `src/lib/sortRows.ts`
- Create: `scripts/sort-check.ts`
- Modify: `package.json:16-22`（scripts 段）

- [ ] **Step 1: 先写断言脚本（此时被测模块还不存在，必然失败）**

创建 `scripts/sort-check.ts`：

```ts
/**
 * 排序比较器断言脚本（本项目无单元测试框架，用这个替代）
 *
 *   npm run check:sort
 *
 * 纯函数断言：不连数据库、不发网络请求、不需要服务在运行。
 * 判定失败以非 0 退出。
 */
import { createComparator, type SortOrder } from "@/lib/sortRows";
import type { Requirement, WorkOrder } from "@/types";

let pass = 0;
let fail = 0;

function check(name: string, actual: unknown, expected: unknown) {
  const a = JSON.stringify(actual);
  const e = JSON.stringify(expected);
  if (a === e) {
    pass += 1;
    console.log(`  [PASS] ${name}`);
  } else {
    fail += 1;
    console.error(`  [FAIL] ${name}\n         实际 ${a}\n         期望 ${e}`);
  }
}

/** 造一条需求，只覆盖排序用到的字段 */
function req(over: Partial<Requirement> & { id: string }): Requirement {
  return {
    requirementId: over.id,
    date: "2026-01-01",
    system: "WMS",
    currentNode: "方案中",
    isUrgent: false,
    isReleased: false,
    createdAt: "2026-01-01T00:00:00.000Z",
    updatedAt: "2026-01-01T00:00:00.000Z",
    ...over,
  } as Requirement;
}

function wo(id: string, status: string): WorkOrder {
  return {
    id,
    status,
    date: "2026-01-01",
    title: id,
    content: "",
    system: "WMS",
    isConvertToRequirement: false,
    createdAt: "2026-01-01T00:00:00.000Z",
    updatedAt: "2026-01-01T00:00:00.000Z",
  } as WorkOrder;
}

function ids<T extends { id: string }>(
  rows: T[],
  field: keyof T,
  order: SortOrder,
): string[] {
  return [...rows].sort(createComparator<T>(field, order)).map((r) => r.id);
}

console.log("=== 日期（YYYY-MM-DD 字典序即时间序）===");
const byDate = [
  req({ id: "b", date: "2026-03-05" }),
  req({ id: "a", date: "2026-01-20" }),
  req({ id: "c", date: "2026-12-01" }),
];
check("升序", ids(byDate, "date", "ascend"), ["a", "b", "c"]);
check("降序", ids(byDate, "date", "descend"), ["c", "b", "a"]);

console.log("=== 开发时长（空值恒排末尾）===");
const byDays = [
  req({ id: "n1", developmentDays: undefined }),
  req({ id: "d30", developmentDays: 30 }),
  req({ id: "d5", developmentDays: 5 }),
  req({ id: "n2", developmentDays: undefined }),
];
check("升序：空值在末尾", ids(byDays, "developmentDays", "ascend"), [
  "d5",
  "d30",
  "n1",
  "n2",
]);
check("降序：空值仍在末尾", ids(byDays, "developmentDays", "descend"), [
  "d30",
  "d5",
  "n1",
  "n2",
]);

console.log("=== 发版时间（未发版者此值为空，占多数）===");
const byRelease = [
  req({ id: "r2", releaseDate: "2026-06-01" }),
  req({ id: "none", releaseDate: undefined }),
  req({ id: "r1", releaseDate: "2026-02-01" }),
];
check("升序", ids(byRelease, "releaseDate", "ascend"), ["r1", "r2", "none"]);
check("降序", ids(byRelease, "releaseDate", "descend"), ["r2", "r1", "none"]);

console.log("=== 当前节点（按 NODE_OPTIONS 业务流程顺序）===");
const byNode = [
  req({ id: "已发布", currentNode: "已发布" }),
  req({ id: "方案中", currentNode: "方案中" }),
  req({ id: "已合并", currentNode: "已合并" }),
  req({ id: "开发中", currentNode: "开发中" }),
  req({ id: "测试中", currentNode: "测试中" }),
];
check("升序=流程推进方向", ids(byNode, "currentNode", "ascend"), [
  "方案中",
  "开发中",
  "测试中",
  "已合并",
  "已发布",
]);
check("降序=流程倒序", ids(byNode, "currentNode", "descend"), [
  "已发布",
  "已合并",
  "测试中",
  "开发中",
  "方案中",
]);

console.log("=== 系统（按 SYSTEM_OPTIONS 声明顺序）===");
const bySystem = [
  req({ id: "TMS", system: "TMS" }),
  req({ id: "WMS", system: "WMS" }),
  req({ id: "其他", system: "其他" }),
  req({ id: "ERP", system: "ERP" }),
];
check("升序=声明顺序", ids(bySystem, "system", "ascend"), [
  "WMS",
  "ERP",
  "TMS",
  "其他",
]);

console.log("=== 布尔字段 ===");
const byUrgent = [
  req({ id: "yes", isUrgent: true }),
  req({ id: "no1", isUrgent: false }),
  req({ id: "no2", isUrgent: false }),
];
check("是否加急 升序：否在前", ids(byUrgent, "isUrgent", "ascend"), [
  "no1",
  "no2",
  "yes",
]);
check("是否加急 降序：是在前", ids(byUrgent, "isUrgent", "descend"), [
  "yes",
  "no1",
  "no2",
]);

console.log("=== 工单状态（按 WORK_ORDER_STATUS_OPTIONS 声明顺序）===");
const byStatus = [
  wo("已关闭", "已关闭"),
  wo("新建", "新建"),
  wo("已处理", "已处理"),
];
check("升序", ids(byStatus, "status", "ascend"), ["新建", "已处理", "已关闭"]);
check("降序", ids(byStatus, "status", "descend"), ["已关闭", "已处理", "新建"]);

console.log("=== 历史脏数据（值不在枚举表内）===");
const dirty = [
  req({ id: "未知节点", currentNode: "未知节点" as never }),
  req({ id: "方案中", currentNode: "方案中" }),
];
check("不在表内的值排末尾", ids(dirty, "currentNode", "ascend"), [
  "方案中",
  "未知节点",
]);

console.log("=== 比较器不修改原数组 ===");
const untouched = [req({ id: "b" }), req({ id: "a" })];
const before = untouched.map((r) => r.id);
[...untouched].sort(createComparator<Requirement>("id", "ascend"));
check("原数组顺序不变", untouched.map((r) => r.id), before);

console.log("");
if (fail === 0) {
  console.log(`全部用例通过 ✅（${pass} 项）`);
} else {
  console.error(`${fail} 项失败 ❌（通过 ${pass} 项）`);
  process.exit(1);
}
```

- [ ] **Step 2: 运行，确认它失败**

Run: `npx tsx scripts/sort-check.ts`
Expected: 报错 `Cannot find module '@/lib/sortRows'`，非 0 退出。这证明断言确实在测新代码，而不是空跑通过。

- [ ] **Step 3: 实现 `src/lib/sortRows.ts`**

创建 `src/lib/sortRows.ts`：

```ts
// 列表排序的比较器。纯函数，不依赖 React 与 antd。
//
// 枚举字段（system / currentNode / status）的业务顺序不另建一套，
// 直接复用 src/types/index.ts 里下拉选项数组的下标 —— 符合项目硬规则第 1 条
// 「下拉选项只有一个来源」。日后调整下拉顺序，排序顺序自动跟随。
import {
  NODE_OPTIONS,
  SYSTEM_OPTIONS,
  WORK_ORDER_STATUS_OPTIONS,
} from "@/types";

export type SortOrder = "ascend" | "descend";

export interface SortState<T> {
  field: keyof T;
  order: SortOrder;
}

/** 字段名 → 该字段的枚举取值顺序表 */
const ENUM_RANK: Record<string, readonly string[]> = {
  system: SYSTEM_OPTIONS,
  currentNode: NODE_OPTIONS,
  status: WORK_ORDER_STATUS_OPTIONS,
};

/** null / undefined / 空串 一律视为无值 */
function isBlank(v: unknown): boolean {
  return v === null || v === undefined || v === "";
}

/**
 * 建一个排序比较器。
 *
 * 空值判定放在升降序取反**之外**：未发版的需求 releaseDate 为空、未填
 * developmentDays 也为空，这些行无论升序降序都排在末尾，不会在降序时
 * 冲到最前面。
 */
export function createComparator<T>(
  field: keyof T,
  order: SortOrder,
): (a: T, b: T) => number {
  const rank = ENUM_RANK[String(field)];

  return (a, b) => {
    const av: unknown = a[field];
    const bv: unknown = b[field];

    const aBlank = isBlank(av);
    const bBlank = isBlank(bv);
    if (aBlank && bBlank) return 0;
    if (aBlank) return 1;
    if (bBlank) return -1;

    let r: number;
    if (rank) {
      // 枚举字段：按下标比业务顺序；不在表内的历史脏数据排末尾
      const ai = rank.indexOf(String(av));
      const bi = rank.indexOf(String(bv));
      if (ai < 0 && bi < 0) return 0;
      if (ai < 0) return 1;
      if (bi < 0) return -1;
      r = ai - bi;
    } else if (typeof av === "number" && typeof bv === "number") {
      r = av - bv;
    } else if (typeof av === "boolean" && typeof bv === "boolean") {
      r = Number(av) - Number(bv);
    } else {
      // 字符串字段：date / releaseDate 出参为 YYYY-MM-DD，字典序即时间序
      const as = String(av);
      const bs = String(bv);
      r = as < bs ? -1 : as > bs ? 1 : 0;
    }

    return order === "ascend" ? r : -r;
  };
}
```

注意 `return 0 / 1 / -1` 那三行用的是 `return` 而非 `r =`：枚举表外的值必须**无视升降序**恒排末尾，所以要在最后的取反之前直接返回。

- [ ] **Step 4: 运行，确认通过**

Run: `npx tsx scripts/sort-check.ts`
Expected: 逐项 `[PASS]`，最后一行 `全部用例通过 ✅（15 项）`，退出码 0。

- [ ] **Step 5: 挂上 npm 脚本**

修改 `package.json`，在 `db:backup` 一行之后插入：

```json
    "check:sort": "tsx scripts/sort-check.ts",
```

即 scripts 段变成：

```json
    "db:seed": "tsx prisma/seed.ts",
    "db:backup": "node scripts/backup-db.mjs",
    "db:reset": "prisma migrate reset --force",
    "check:env": "node scripts/check-env.mjs",
    "check:sort": "tsx scripts/sort-check.ts",
    "api:smoke": "node scripts/api-smoke.mjs",
```

- [ ] **Step 6: 跑两个自动化验证**

Run: `npm run check:sort && npm run typecheck`
Expected: 前者 15 项全过；后者无任何输出（`tsc --noEmit` 成功时静默）。

- [ ] **Step 7: 提交**

```bash
git add src/lib/sortRows.ts scripts/sort-check.ts package.json
git commit -m "新增列表排序比较器与断言脚本

排序比较器处理四类字段（数值/布尔/字符串/枚举）与空值语义。
枚举字段的序直接复用 src/types 现有选项数组的下标，不另建一套顺序，
符合项目硬规则第 1 条。

空值判定放在升降序取反之外，保证未发版需求的 releaseDate、未填的
developmentDays 无论升序降序都排在末尾。

项目无单元测试框架，用 scripts/sort-check.ts 以断言脚本替代，
npm run check:sort 运行，纯函数不碰数据库与网络。"
```

---

## Task 2: 可排序列头组件 `src/components/common/SortableHeader.tsx`

**Files:**
- Create: `src/components/common/SortableHeader.tsx`

**说明：** 这是纯展示组件，本项目无 React 测试框架，**无法自动化验证**。本任务的验证手段是 `npm run typecheck`，实际交互留到 Task 7 由用户在浏览器确认。

- [ ] **Step 1: 创建组件**

创建 `src/components/common/SortableHeader.tsx`：

```tsx
"use client";

import type { ReactNode } from "react";
import type { SortState } from "@/lib/sortRows";

export interface SortableHeaderProps<T> {
  /** 列名文本 */
  label: ReactNode;
  /** 该列对应的排序字段 */
  field: keyof T;
  /** 当前排序状态；null 表示未排序 */
  sort: SortState<T> | null;
  /** 双击时回调，由页面负责三态循环 */
  onToggle: (field: keyof T) => void;
}

/**
 * 可排序的列头：**双击**切换排序。
 *
 * 为什么不用 antd 内置 sorter 默认的单击：
 *   本项目的 ProTable 表头已用于拖拽调整列顺序（ProTable.tsx 的 HeaderCell）。
 *   若排序是单击触发，「按下鼠标准备拖列、但没移动就松手」会被判成一次点击，
 *   从而误触发排序。双击把两个手势分开。
 *
 * 指示符画在列名**左侧**：右侧要留给 ProTable 右缘那个 6px 的拖宽手柄
 * （z-index: 1），画在右边会被它盖住。
 *
 * 未排序时显示灰色 ⇅，用来提示该列「可排序」—— 双击手势不像单击那样
 * 一眼能看出来，需要这个视觉线索。
 */
export default function SortableHeader<T>({
  label,
  field,
  sort,
  onToggle,
}: SortableHeaderProps<T>) {
  const active = sort !== null && sort.field === field;
  const order = active && sort ? sort.order : null;

  return (
    <span
      onDoubleClick={() => onToggle(field)}
      title="双击排序：升序 → 降序 → 取消"
      style={{ cursor: "pointer", userSelect: "none" }}
    >
      <span
        aria-hidden="true"
        style={{
          display: "inline-block",
          width: 10,
          marginRight: 2,
          fontSize: 10,
          color: active ? "#1677ff" : "#bfbfbf",
        }}
      >
        {order === "ascend" ? "▲" : order === "descend" ? "▼" : "⇅"}
      </span>
      {label}
    </span>
  );
}
```

`#1677ff` 与 `src/app/layout.tsx:31` 的 `colorPrimary` 一致。

- [ ] **Step 2: 类型检查**

Run: `npm run typecheck`
Expected: 无输出（通过）。

- [ ] **Step 3: 提交**

```bash
git add src/components/common/SortableHeader.tsx
git commit -m "新增可排序的列头组件（双击触发）

指示符画在列名左侧，避开 ProTable 右缘 6px 的拖宽手柄；
未排序时显示灰色 ⇅ 提示该列可排序。"
```

---

## Task 3: 需求列表页接入排序

**Files:**
- Modify: `src/app/requirements/page.tsx`

- [ ] **Step 1: 加 import**

在 `src/app/requirements/page.tsx:30-32` 附近，把：

```tsx
import ProTable from "@/components/common/ProTable";
import EditableChip from "@/components/common/EditableChip";
import { useRowDrafts } from "@/lib/useRowDrafts";
```

改为：

```tsx
import ProTable from "@/components/common/ProTable";
import EditableChip from "@/components/common/EditableChip";
import SortableHeader from "@/components/common/SortableHeader";
import { createComparator, type SortState } from "@/lib/sortRows";
import { useRowDrafts } from "@/lib/useRowDrafts";
```

- [ ] **Step 2: 加排序状态与派生数据**

在 `src/app/requirements/page.tsx:57-58` 的筛选状态之后、`useRowDrafts` 之前，插入：

```tsx
  // 排序状态：null = 未排序，保持后端返回的 createdAt 倒序
  const [sort, setSort] = useState<SortState<Requirement> | null>(null);
```

在 `filteredRequirements` 的 `useMemo`（原第 70-80 行）**之后**，插入：

```tsx
  // 双击列头：升序 → 降序 → 取消（回到默认的创建时间倒序）
  const handleSortToggle = (field: keyof Requirement) => {
    setSort((prev) => {
      if (!prev || prev.field !== field) return { field, order: "ascend" };
      if (prev.order === "ascend") return { field, order: "descend" };
      return null;
    });
  };

  // 排序作用在已提交值上（在 drafts.merge 之前）：
  // 未提交的内联草稿不参与排序，避免用户编辑时那一行跳走。
  const sortedRequirements = useMemo(() => {
    if (!sort) return filteredRequirements;
    return [...filteredRequirements].sort(
      createComparator<Requirement>(sort.field, sort.order),
    );
  }, [filteredRequirements, sort]);

  // 列头工厂：给可排序的列统一挂上双击行为
  const sortableTitle = (label: string, field: keyof Requirement) => (
    <SortableHeader<Requirement>
      label={label}
      field={field}
      sort={sort}
      onToggle={handleSortToggle}
    />
  );
```

- [ ] **Step 3: 给 6 个列头套上排序**

在 `columns` 数组里做 6 处替换（只改 `title`，其余属性一律不动）：

| 原 | 改为 |
|----|------|
| `title: "日期",`（第 110 行） | `title: sortableTitle("日期", "date"),` |
| `title: "开发时长(天)",`（第 152 行） | `title: sortableTitle("开发时长(天)", "developmentDays"),` |
| `title: "当前节点",`（第 160 行） | `title: sortableTitle("当前节点", "currentNode"),` |
| `title: "是否加急",`（第 179 行） | `title: sortableTitle("是否加急", "isUrgent"),` |
| `title: "是否发版",`（第 197 行） | `title: sortableTitle("是否发版", "isReleased"),` |
| `title: "发版时间",`（第 224 行） | `title: sortableTitle("发版时间", "releaseDate"),` |

**不要**给「需求ID」「需求标题」「需求内容」「系统」「来源工单」「备注」「操作」加排序。

> 行号会随 Step 2 的插入而偏移，以 `title:` 的原文内容为准定位。

- [ ] **Step 4: 把排序结果接进表格**

把 `src/app/requirements/page.tsx` 的 ProTable 改为（只改 `dataSource` 一行）：

```tsx
      <ProTable<Requirement>
        rowKey="id"
        columns={columns}
        dataSource={sortedRequirements.map(drafts.merge)}
        loading={loading}
        scroll={{ x: 1560 }}
        layoutStorageKey="requirements"
        pagination={{
          pageSize: 10,
          showSizeChanger: false,
          showTotal: (total) => `共 ${total} 条`,
        }}
      />
```

**分页保持现状**：不做受控分页，排序后停留在当前页码（已与用户确认）。

- [ ] **Step 5: 类型检查**

Run: `npm run typecheck`
Expected: 无输出（通过）。

- [ ] **Step 6: 提交**

```bash
git add src/app/requirements/page.tsx
git commit -m "需求列表支持双击列名排序

可排序字段：日期 / 开发时长 / 当前节点 / 是否加急 / 是否发版 / 发版时间。
排序作用在 drafts.merge 之前，按已提交值排，编辑中的行不会跳走。
分页保持前端非受控，排序后停留当前页。"
```

---

## Task 4: 工单列表页接入排序

**Files:**
- Modify: `src/app/work-orders/page.tsx`

- [ ] **Step 1: 加 import**

在 `src/app/work-orders/page.tsx` 的 import 区（`ProTable` / `EditableChip` 那几行附近）加入：

```tsx
import SortableHeader from "@/components/common/SortableHeader";
import { createComparator, type SortState } from "@/lib/sortRows";
```

- [ ] **Step 2: 加排序状态与派生数据**

在该页筛选状态定义之后、`useRowDrafts` 之前插入：

```tsx
  // 排序状态：null = 未排序，保持后端返回的 createdAt 倒序
  const [sort, setSort] = useState<SortState<WorkOrder> | null>(null);
```

在 `filteredWorkOrders` 的 `useMemo` **之后**插入：

```tsx
  // 双击列头：升序 → 降序 → 取消（回到默认的创建时间倒序）
  const handleSortToggle = (field: keyof WorkOrder) => {
    setSort((prev) => {
      if (!prev || prev.field !== field) return { field, order: "ascend" };
      if (prev.order === "ascend") return { field, order: "descend" };
      return null;
    });
  };

  // 排序作用在已提交值上（在 drafts.merge 之前）
  const sortedWorkOrders = useMemo(() => {
    if (!sort) return filteredWorkOrders;
    return [...filteredWorkOrders].sort(
      createComparator<WorkOrder>(sort.field, sort.order),
    );
  }, [filteredWorkOrders, sort]);

  // 列头工厂：给可排序的列统一挂上双击行为
  const sortableTitle = (label: string, field: keyof WorkOrder) => (
    <SortableHeader<WorkOrder>
      label={label}
      field={field}
      sort={sort}
      onToggle={handleSortToggle}
    />
  );
```

- [ ] **Step 3: 给 3 个列头套上排序**

在 `columns` 数组里做 3 处替换（只改 `title`）：

| 原 | 改为 |
|----|------|
| `title: "日期",`（第 143 行） | `title: sortableTitle("日期", "date"),` |
| `title: "系统",`（第 162 行） | `title: sortableTitle("系统", "system"),` |
| `title: "状态",`（第 203 行） | `title: sortableTitle("状态", "status"),` |

注意**只改这三处**：工单表单页（`src/components/WorkOrderForm.tsx`）里也有 `title: "系统"` 之类的字样，别改错文件。本步只在 `src/app/work-orders/page.tsx` 内操作。

- [ ] **Step 4: 接进表格**

把该页 ProTable（原第 340-352 行）的 `dataSource` 改为：

```tsx
      <ProTable<WorkOrder>
        rowKey="id"
        columns={columns}
        dataSource={sortedWorkOrders.map(drafts.merge)}
        loading={loading}
        scroll={{ x: 1510 }}
        layoutStorageKey="work-orders"
        pagination={{
          pageSize: 10,
          showSizeChanger: false,
          showTotal: (total) => `共 ${total} 条`,
        }}
      />
```

- [ ] **Step 5: 转需求弹框的 TextArea 高度翻倍**

该页「工单转需求」弹框里第 391-396 行的裸 `TextArea`，把 `rows={3}` 改为 `rows={6}`：

```tsx
            <div style={{ marginBottom: 6 }}>需求内容</div>
            <Input.TextArea
              rows={6}
              placeholder="选填，描述需求背景与验收期望"
              value={convertContent}
              onChange={(e) => setConvertContent(e.target.value)}
            />
```

这处**只加倍高度，不动宽度**——它不在表单的 `maxWidth` 容器内，宽度本来就撑满弹框。

- [ ] **Step 6: 类型检查**

Run: `npm run typecheck`
Expected: 无输出（通过）。

- [ ] **Step 7: 提交**

```bash
git add src/app/work-orders/page.tsx
git commit -m "工单列表支持双击列名排序 + 转需求弹框内容框加高

可排序字段：日期 / 系统 / 状态。
转需求弹框的需求内容 rows 3 → 6（该处不在表单容器内，宽度不变）。"
```

---

## Task 5: 表单内容框加宽加高

**Files:**
- Modify: `src/components/WorkOrderForm.tsx`
- Modify: `src/components/RequirementForm.tsx`

**背景：** 宽度不由 `Input.TextArea` 决定，而是被外层 `<div style={{ maxWidth: 640 }}>` 卡住。所以改容器，不给 TextArea 加 `style.width`。

- [ ] **Step 1: 工单表单——改容器宽度**

`src/components/WorkOrderForm.tsx` 第 208 行：

```tsx
      <div style={{ maxWidth: 640 }}>
```

改为：

```tsx
      <div style={{ maxWidth: 960 }}>
```

同步修改紧邻的上方注释（第 207 行），把「限制表单列宽，避免整卡宽度下输入框过长」保留，并在其后补一句说明当前值：

```tsx
      {/* 限制表单列宽，避免整卡宽度下输入框过长；当前 960px */}
```

- [ ] **Step 2: 工单表单——3 处 rows 翻倍**

同一文件内按内容定位（不要只靠行号）：

| 定位依据 | 原 | 改为 |
|----------|-----|------|
| `label="工单内容"` 的 `Input.TextArea` | `rows={4}` | `rows={8}` |
| `name="requirementContent"` 的 `Input.TextArea` | `rows={3}` | `rows={6}` |
| `label="备注"` 的 `Input.TextArea` | `rows={2}` | `rows={4}` |

改完后 `label="工单内容"` 那处应为：

```tsx
        <Form.Item
          label="工单内容"
          name="content"
          rules={[
            { required: true, whitespace: true, message: "请输入工单内容" },
          ]}
        >
          <Input.TextArea
            rows={8}
            placeholder="请描述工单内容、背景与期望结果"
          />
        </Form.Item>
```

- [ ] **Step 3: 工单表单——更新 WIDTH 常量的注释**

第 74-76 行附近的注释写明了「表单容器统一 640px」，改为 960px，否则注释与代码不符。

- [ ] **Step 4: 需求表单——改容器宽度**

`src/components/RequirementForm.tsx` 第 247 行：

```tsx
      <div style={{ maxWidth: 640 }}>
```

改为：

```tsx
      <div style={{ maxWidth: 960 }}>
```

并同步第 246 行的行内注释为 `{/* 限制表单列宽，避免整卡宽度下输入框过长；当前 960px */}`。

- [ ] **Step 5: 需求表单——2 处 rows 翻倍**

| 定位依据 | 原 | 改为 |
|----------|-----|------|
| `label="需求内容"` 的 `Input.TextArea` | `rows={3}` | `rows={6}` |
| `label="备注"` 的 `Input.TextArea` | `rows={2}` | `rows={4}` |

改完后 `label="需求内容"` 那处应为：

```tsx
        <Form.Item label="需求内容" name="content">
          <Input.TextArea
            rows={6}
            placeholder="选填，描述需求背景、范围与验收期望"
          />
        </Form.Item>
```

- [ ] **Step 6: 需求表单——更新 WIDTH 常量的注释**

第 120-122 行附近同样写着「表单容器统一 640px」，改为 960px。

- [ ] **Step 7: 确认没有漏网的 640**

Run: `grep -rn "maxWidth: 640\|640px" src/`

Expected: 无输出。若还有命中，说明注释或容器漏改。

- [ ] **Step 8: 类型检查**

Run: `npm run typecheck`
Expected: 无输出（通过）。

- [ ] **Step 9: 提交**

```bash
git add src/components/WorkOrderForm.tsx src/components/RequirementForm.tsx
git commit -m "表单内容框加宽 1/2、高度翻倍

宽度由外层 maxWidth 容器决定而非 TextArea 自身，故将容器从 640 改为 960
（撑满容器的标题/需求ID 等 Input 一并变宽；日期/系统等固定像素下拉框不受影响）。
内容框与备注框的 rows 翻倍，并同步更新两处写明「640px」的注释。

新建页与编辑页共用这两个表单组件，所以两处同时生效。"
```

---

## Task 6: 文档回写

**Files:**
- Modify: `docs/四川物流工单系统-设计方案.md`
- Modify: `docs/用户手册.md`

**依据：** 项目硬规则——`docs/四川物流工单系统-设计方案.md` 是唯一权威设计文档，功能新增需回写。

- [ ] **Step 1: 先读现状再改**

Run: `grep -n "高级搜索\|功能范围\|列表\|排序" docs/四川物流工单系统-设计方案.md | head -40`

先看清楚功能范围表与待办事项的实际措辞，**按既有格式补一行**，不要新起章节结构。此前该文件第 461 行附近把「高级搜索和筛选」列在待办中，需按本次实际情况调整措辞。

- [ ] **Step 2: 回写设计方案**

在功能范围表中，按该表既有列格式，新增一行描述「列表按字段排序」：双击列名，升序 → 降序 → 取消；需求列表可排字段为 日期/开发时长/当前节点/是否加急/是否发版/发版时间，工单列表为 日期/系统/状态；枚举字段按业务流程顺序（取自 `src/types/index.ts` 的选项数组下标）。

- [ ] **Step 3: 回写用户手册**

`docs/用户手册.md` 的「3.7 自定义列的显示顺序与宽度」章节（约 126-130 行）讲的是拖列宽列序。在其后补一小节说明排序操作，风格与该手册一致（面向使用者、步骤化）：

- 双击列名排序，再双击切换到降序，第三次双击取消排序
- 列名左侧的 ⇅ 表示该列可排序；▲ 升序、▼ 降序
- 排序只改变显示顺序，不改动任何数据
- 有未提交的内联修改时，排序按「已提交的值」计算，未提交的改动不影响行位置

同时在 Q&A 章节（约 264 行「列表列的顺序乱了」附近）补一条排序相关的问答。

- [ ] **Step 4: 检查有没有需要同步的具体数字**

Run: `grep -rn "107" docs/用户手册.md docs/四川物流工单系统-设计方案.md docs/API-接口文档.md`

本次**不涉及接口变更**，`docs/API-接口文档.md` 与冒烟断言数（107）**都不需要改**。若上述 grep 命中与断言数相关的描述，确认后保持原样即可。

- [ ] **Step 5: 提交**

```bash
git add docs/四川物流工单系统-设计方案.md docs/用户手册.md
git commit -m "文档回写：列表排序功能

设计方案功能范围表新增「列表按字段排序」；用户手册补充双击排序的
操作说明与 Q&A。本次不涉及接口变更，API 文档与冒烟断言数不变。"
```

---

## Task 7: 用户在浏览器中验证（交付门槛）

**依据：** 项目硬规则第 3 条——**UI 改动必须由用户在浏览器中实际验证后才算完成**，本环境无浏览器自动化。以下各项**不能由实施者代为勾选**。

**前置：** 先停掉可能占用 `.next` 的 dev server，然后：

```bash
npm run build && npm run start
```

浏览器打开 http://localhost:3000

- [ ] **V1. 双击能触发排序** —— 进「需求列表」，双击「日期」列名。这一项在计划里标为最高风险：ProTable 把表头内容包在一个 `draggable` 的 div 里（`ProTable.tsx:158-173`），双击事件能否穿透到 SortableHeader 需要实测。

- [ ] **V2. 两套手势不打架** —— 在同一个表头上：① 按住拖动 → 应调整为**列顺序**，且不触发排序；② 双击 → 应触发**排序**，且不改变列顺序。

- [ ] **V3. 指示符没被遮挡** —— 排序后列名左侧应出现 ▲ 或 ▼，且清晰可见。右侧 6px 的拖宽手柄不应盖住它。

- [ ] **V4. 三态循环正确** —— 连续双击同一列名三次：升序 ▲ → 降序 ▼ → 恢复未排序（顺序回到创建时间倒序）。双击另一列名时，应从该列的升序重新开始。

- [ ] **V5. 排序语义正确** —— 重点验「当前节点」列：升序应是 方案中 → 开发中 → 测试中 → 已合并 → 已发布（业务流程顺序），而**不是**拼音序。「开发时长」和「发版时间」列的空值应**无论升降序都排在末尾**。

- [ ] **V6. 编辑中的行不跳走** —— 在内联编辑某行的「当前节点」但**不提交**，然后点排序：该行位置应保持不动（按已提交值排）。点「提交」后，该行才移动到新位置。

- [ ] **V7. 工单列表同样可用** —— 重复 V1-V4，「日期/系统/状态」三列。

- [ ] **V8. 表单尺寸** —— 进工单新建页与编辑页：内容框 `rows` 翻倍、宽度加宽 1/2，标题等字段一并变宽，**页面无横向滚动条溢出**。需求新建/编辑页同样。工单列表「转需求」弹框里的需求内容框高度翻倍。

- [ ] **V9. 无回归** —— 列表的筛选（关键字、节点/系统下拉）仍正常；内联编辑、提交、还原、删除、转需求等原有功能不受影响。

**若 V1 失败**（双击不触发）：不要改用单击——先回报，需要重新设计列头的挂载方式（例如不放在 `title` 里，而是自定义 `components.header.cell`，但那会与 ProTable 现有的 `HeaderCell` 冲突，需要另行设计）。

---

## 完工标准

- `npm run check:sort` 15 项全过
- `npm run typecheck` 无错误
- Task 7 的 V1-V9 全部由**用户**确认通过
- `git status` 干净（`启动系统.bat` 按约定不入库，保持未跟踪）

---

## 明确不做（防止范围蔓延）

- 不做后端排序（不改接口、不改 `store.ts`、不改 `api.ts`）
- 不做受控分页（排序后停留当前页）
- 不持久化排序状态到 localStorage
- 不改列宽列序的 localStorage 持久化
- 不给文本列（需求ID/标题/内容/备注）与派生列（来源工单/关联需求/操作）加排序
- 不改动 `EditableChip` 与 `useRowDrafts` 的机制
