# 系统选项增删 + 枚举兼容保险 + 每页条数选择器 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 系统下拉框删 OMS、增 SCM 与追溯中心；给 `system` / `status` 加枚举兼容保险，使删除选项后存量记录仍可编辑；两个列表页支持切换每页条数并记住选择。

**Architecture:** 系统选项只改 `src/types/index.ts`（项目硬规则第 1 条：下拉选项唯一来源），其余消费点自动跟随。枚举保险改在两个 PUT 路由的校验分支：提交值等于库中现值时跳过枚举校验。每页条数由 `ProTable` 统一接管，存独立的 localStorage key，只接管 `pageSize` 不接管 `current`。

**Tech Stack:** Next.js 14 App Router / TypeScript 5 / antd 5 / Prisma 5 + SQLite

**设计依据：** `docs/superpowers/specs/2026-09-16-系统选项与分页条数-design.md`

---

## 项目前提（实施者必读）

- **本项目没有单元测试框架**。自动化验证共 4 个命令：`npm run check:sort`（15 项纯函数断言）、
  `npm run typecheck`、`npm run build`、`npm run api:smoke`（后端接口冒烟，需服务在 3000 端口运行）。
- **`api:smoke` 本次必须跑** —— 动了后端校验逻辑，且要新增断言。
- **UI 交互无法自动验证**，项目硬规则第 3 条规定必须由用户在浏览器实测。Task 8 是交付门槛。
- Windows 上服务运行时锁 `.next`，**构建前必须先停服**，否则 `npm run build` 报 EPERM。

### 停服的正确做法（Windows 坑，CLAUDE.md 有记录）

`npm run start` 会拉起两个进程（npm 包装进程 + next 服务），停掉外层后内层可能仍在监听：

```bash
netstat -ano | grep LISTENING | grep ":3000"     # 拿 PID
taskkill //F //PID <PID>
```

---

## 文件结构

| 文件 | 改动 | 职责 |
|------|------|------|
| `src/types/index.ts` | 修改 | 系统选项与配色的唯一定义处，外加一处不实注释的修正 |
| `src/app/api/work-orders/[id]/route.ts` | 修改 | 工单 PUT 的 `system` / `status` 校验 |
| `src/app/api/requirements/[id]/route.ts` | 修改 | 需求 PUT 的 `system` 校验 |
| `scripts/api-smoke.mjs` | 修改 | 补回归断言，锁住保险逻辑 |
| `src/components/common/ProTable.tsx` | 修改 | 接管每页条数：默认值、选择器、持久化 |
| `src/app/work-orders/page.tsx` | 修改 | 移除自带的 `pageSize` / `showSizeChanger` |
| `src/app/requirements/page.tsx` | 修改 | 同上 |
| `docs/用户手册.md` / `docs/API-接口文档.md` | 修改 | 系统取值硬编码处同步 |

`docs/四川物流工单系统-设计方案.md` **不需要改**（核实过它从未枚举系统取值）。

---

## Task 1: 系统选项增删

**Files:**
- Modify: `src/types/index.ts`

- [ ] **Step 1: 改 SYSTEM_OPTIONS**

`src/types/index.ts` 第 23 行：

```ts
export const SYSTEM_OPTIONS = ["WMS", "ERP", "OMS", "TMS", "其他"] as const;
```

改为：

```ts
export const SYSTEM_OPTIONS = ["WMS", "ERP", "TMS", "SCM", "追溯中心", "其他"] as const;
```

删 `OMS`，增 `SCM` 与 `追溯中心`，两者追加在「其他」之前。
**顺序有意义**：数组下标同时决定「系统」列的排序权重（`src/lib/sortRows.ts` 的
`ENUM_RANK` 用 `indexOf`）。追加而非顶替 OMS 的位置，可保持既有选项相对顺序不变，
且「其他」稳定排在最后。

- [ ] **Step 2: 同步 SYSTEM_COLORS**

同文件第 62-68 行：

```ts
export const SYSTEM_COLORS: Record<string, string> = {
  WMS: "blue",
  ERP: "purple",
  OMS: "cyan",
  TMS: "orange",
  其他: "gold",
};
```

改为：

```ts
export const SYSTEM_COLORS: Record<string, string> = {
  WMS: "blue",
  ERP: "purple",
  TMS: "orange",
  SCM: "green",
  追溯中心: "magenta",
  其他: "gold",
};
```

> 该表目前**未被消费**：`EditableChip` 虽然声明了 `colorOf` prop，但函数签名解构时
> 没有取出它，文字颜色是硬编码的。此处同步只为保持文件自洽，**不产生任何视觉变化**。

- [ ] **Step 3: 修正顶部一处不实注释**

同文件第 15 行：

```ts
//   - 配色表是 Record<联合类型, string>，漏加配色会直接编译报错，不会静默失败；
```

改为：

```ts
//   - 节点与工单状态的配色表是 Record<联合类型, string>，漏加配色会直接编译报错；
//     但系统的配色表 SYSTEM_COLORS 是宽松的 Record<string, string>，漏加**不会**报错，
//     会静默退回默认色（该表当前未被消费，见其定义处的说明）；
```

原因：`SYSTEM_COLORS` 的实际类型是 `Record<string, string>`，原注释对系统字段不成立。

- [ ] **Step 4: 验证排序断言未被影响**

Run: `npm run check:sort`
Expected: `全部用例通过 ✅（15 项）`。
断言里只硬编码了 WMS/ERP/TMS/其他 四者的**相对**顺序，新选项不影响。

- [ ] **Step 5: 类型检查**

Run: `npm run typecheck`
Expected: 无输出（零错误）。

- [ ] **Step 6: 提交**

```bash
git add src/types/index.ts
git commit -m "系统下拉选项：删 OMS，增 SCM 与追溯中心

两个新选项追加在「其他」之前而非顶替 OMS 位置 —— 数组下标同时决定
「系统」列的排序权重，追加可保持既有选项相对顺序不变，「其他」也稳定居末。

SYSTEM_COLORS 同步（该表当前未被消费，EditableChip 已忽略 colorOf，
故无视觉变化）。同时修正顶部一处不实注释：SYSTEM_COLORS 是宽松的
Record<string, string>，漏加配色并不会编译报错。"
```

---

## Task 2: 枚举兼容保险（system + status）

**Files:**
- Modify: `src/app/api/work-orders/[id]/route.ts:69-85`
- Modify: `src/app/api/requirements/[id]/route.ts:87-92`

**要解决的问题**：后端硬校验 `SYSTEMS.includes(system)`，而前端表单保存时**无条件
下发全部字段**。两者叠加 → 一旦删除某个选项且库中有用该值的存量记录，那些记录的
编辑页就再也保存不了（400，且同一笔请求里改的其它字段也一起不落库，因为校验在事务之前）。

- [ ] **Step 1: 改工单 route 的 system 校验**

`src/app/api/work-orders/[id]/route.ts` 第 69-74 行：

```ts
  if (body.system !== undefined) {
    const system = asTrimmed(body.system);
    if (!system || !SYSTEMS.includes(system)) {
      errors.push(`system 须为以下之一：${SYSTEMS.join(" / ")}。`);
    } else data.system = system;
  }
```

改为：

```ts
  if (body.system !== undefined) {
    const system = asTrimmed(body.system);
    // 与库中现值相同 → 视为未变更，跳过枚举校验。
    // 这样管理员删掉某个系统选项后，存量记录的其它字段仍能正常保存；
    // 但把该字段改成枚举外的**新**值仍会被拦下。
    if (!system) {
      errors.push(`system 须为以下之一：${SYSTEMS.join(" / ")}。`);
    } else if (system !== existing.system && !SYSTEMS.includes(system)) {
      errors.push(`system 须为以下之一：${SYSTEMS.join(" / ")}。`);
    } else {
      data.system = system;
    }
  }
```

`existing` 是本路由第 40-42 行已查出的当前记录（PUT 本来就要先查以决定 404），此处可直接用。

- [ ] **Step 2: 改工单 route 的 status 校验**

同文件第 80-85 行：

```ts
  if (body.status !== undefined) {
    const status = asTrimmed(body.status);
    if (!status || !WORK_ORDER_STATUSES.includes(status)) {
      errors.push(`status 须为以下之一：${WORK_ORDER_STATUSES.join(" / ")}。`);
    } else data.status = status;
  }
```

改为：

```ts
  if (body.status !== undefined) {
    const status = asTrimmed(body.status);
    // 规则同 system —— 工单状态同样可由管理员增删，存在完全相同的隐患
    if (!status) {
      errors.push(`status 须为以下之一：${WORK_ORDER_STATUSES.join(" / ")}。`);
    } else if (status !== existing.status && !WORK_ORDER_STATUSES.includes(status)) {
      errors.push(`status 须为以下之一：${WORK_ORDER_STATUSES.join(" / ")}。`);
    } else {
      data.status = status;
    }
  }
```

- [ ] **Step 3: 改需求 route 的 system 校验**

`src/app/api/requirements/[id]/route.ts` 第 87-92 行：

```ts
  if (body.system !== undefined) {
    const system = asTrimmed(body.system);
    if (!system || !SYSTEMS.includes(system)) {
      errors.push(`system 须为以下之一：${SYSTEMS.join(" / ")}。`);
    } else data.system = system;
  }
```

改为：

```ts
  if (body.system !== undefined) {
    const system = asTrimmed(body.system);
    // 规则同工单路由：值未变更时跳过枚举校验
    if (!system) {
      errors.push(`system 须为以下之一：${SYSTEMS.join(" / ")}。`);
    } else if (system !== existing.system && !SYSTEMS.includes(system)) {
      errors.push(`system 须为以下之一：${SYSTEMS.join(" / ")}。`);
    } else {
      data.system = system;
    }
  }
```

`existing` 是该路由第 45-47 行已查出的当前需求。

- [ ] **Step 4: 需求 route 的 currentNode 保持不动**

**明确不改** `currentNode` 的校验（同文件第 93-98 行）。它的五个取值写死在
`docs/四川物流工单系统-设计方案.md` 里，增删属需求变更、需先改设计文档，
不会被随手增删，因此不需要这道保险。

- [ ] **Step 5: 类型检查**

Run: `npm run typecheck`
Expected: 无输出（零错误）。

- [ ] **Step 6: 提交**

```bash
git add src/app/api/work-orders/\[id\]/route.ts src/app/api/requirements/\[id\]/route.ts
git commit -m "枚举校验加兼容保险：值未变更时跳过校验

原先后端对 system / status 硬校验，而前端表单保存时无条件下发全部字段，
两者叠加导致：删除某个选项后，库中用该值的存量记录在编辑页再也保存不了
（400，且同一笔 PUT 里改的其它字段也一起不落库 —— 校验在事务之前）。

改为「提交值与库中现值相同时跳过枚举校验」：存量记录仍可正常编辑其它字段，
但把该字段改成枚举外的新值仍会被拦下。

覆盖 system（两个路由）与 status（仅工单路由）。currentNode 刻意不改 ——
它的取值受设计文档管控，增删属需求变更。"
```

---

## Task 3: 冒烟脚本补回归断言

**Files:**
- Modify: `scripts/api-smoke.mjs`

**目的**：锁住 Task 2 的规则，防止日后改动把「值未变更时跳过校验」这个分支误删。
注意纯 HTTP **造不出**非法存量值，所以这里只做回归（正常路径没被改坏），核心场景
由 Task 6 的手动验证覆盖。

- [ ] **Step 1: 补工单侧断言**

`scripts/api-smoke.mjs` 第 118 行（`// 7b.` 那行）之前插入：

```js
// 7a-1. 枚举兼容保险：传未变更的合法 system 仍可保存（回归）
{
  const cur = await req("GET", `/api/work-orders/${woA}`);
  const r = await req("PUT", `/api/work-orders/${woA}`, {
    system: cur.data.system,
    remark: "保险回归",
  });
  ok(r.status === 200, "传未变更的 system → 200（回归）", `status=${r.status}`);
}
// 7a-2. 枚举兼容保险：改成枚举外的新值仍 400
{
  const r = await req("PUT", `/api/work-orders/${woA}`, {
    system: "不存在的系统",
  });
  ok(r.status === 400, "改成枚举外的 system → 400", `status=${r.status}`);
}
// 7a-3. 枚举兼容保险：传未变更的合法 status 仍可保存（回归）
{
  const cur = await req("GET", `/api/work-orders/${woA}`);
  const r = await req("PUT", `/api/work-orders/${woA}`, {
    status: cur.data.status,
    remark: "保险回归2",
  });
  ok(r.status === 200, "传未变更的 status → 200（回归）", `status=${r.status}`);
}
```

> 新增 3 条。第 4 个方向（status 改成枚举外的新值 → 400）**已由现有断言覆盖**，
> 见同文件第 115-116 行「更新非法状态 → 400」。

- [ ] **Step 2: 补需求侧断言**

同文件第 185 行（需求 CRUD 第 9 节结束的 `}` 之后）插入：

```js
// 9b. 枚举兼容保险：需求侧 system 传未变更值仍可保存（回归）
{
  const r = await req("PUT", `/api/requirements/${reqStandalone}`, {
    system: "WMS",
    remark: "保险回归3",
  });
  ok(r.status === 200, "需求传未变更的 system → 200（回归）", `status=${r.status}`);
}
// 9c. 枚举兼容保险：需求侧 system 改成枚举外的新值仍 400
{
  const r = await req("PUT", `/api/requirements/${reqStandalone}`, {
    system: "不存在的系统",
  });
  ok(r.status === 400, "需求改枚举外的 system → 400", `status=${r.status}`);
}
```

`reqStandalone` 在第 180 行已赋值为刚创建需求的 id；该需求在创建时
`system: "WMS"`（第 174 行），所以 9b 传 `"WMS"` 正是「未变更」的情形。

> 共新增 **5 条**断言（设计文档 2.5 节写的是 4 条，此处为 5 条——因为必须同时覆盖
> 两个路由，工单 3 条 + 需求 2 条）。断言总数 107 → **112**。

- [ ] **Step 3: 停服、重建、启服**

改的是后端代码，必须重新构建后冒烟才有意义。

```bash
netstat -ano | grep LISTENING | grep ":3000"     # 拿 PID
taskkill //F //PID <PID>
npm run build
```

Expected: 构建成功，13 条路由。

- [ ] **Step 4: 启动服务**

```bash
npm run start
```

Expected: `✓ Ready`，监听 3000。保持该窗口开启。

- [ ] **Step 5: 跑冒烟**

Run: `npm run api:smoke`
Expected: 逐项 `[PASS]`，最后 `全部用例通过 ✅`，退出码 0，且能看到新增的
「传未变更的 system → 200（回归）」等 5 条。

- [ ] **Step 6: 提交**

```bash
git add scripts/api-smoke.mjs
git commit -m "冒烟补 5 条枚举兼容保险的回归断言（107 → 112）

工单 3 条（system 未变更→200、system 枚举外→400、status 未变更→200）
+ 需求 2 条（system 未变更→200、system 枚举外→400）。
status 改成枚举外值→400 已由原有断言覆盖，不重复添加。

纯 HTTP 造不出非法存量值，故此处只做回归；核心场景见手动验证步骤。"
```

---

## Task 4: ProTable 接管每页条数

**Files:**
- Modify: `src/components/common/ProTable.tsx`

- [ ] **Step 1: 加档位常量**

`src/components/common/ProTable.tsx` 第 34 行 `type RecordTypeOf = object;` 之后插入：

```ts
/** 每页条数可选档位。必须包含默认值 10，否则 antd 的 Select 无法高亮当前项 */
const PAGE_SIZE_CHOICES = [10, 20, 30, 50, 100];
const DEFAULT_PAGE_SIZE = 10;
```

- [ ] **Step 2: 更新文件头注释**

同文件第 13-17 行的注释块末尾（`存储格式：...` 那行之后）追加：

```
 * 另外会接管**每页条数**：默认 10，可选 10/20/30/50/100，用户的选择存入
 * `protable:pageSize:<key>`（与列布局分开存，避免影响「还原默认列布局」的语义）。
 * 只接管 pageSize，**不接管当前页码** —— 排序后仍停留当前页。
 * 若调用方显式传了 pagination.pageSize，则以调用方为准，本组件不介入。
```

- [ ] **Step 3: 把 pagination 从 rest 中解构出来**

同文件第 194-199 行的函数签名：

```tsx
export default function ProTable<RecordType extends RecordTypeOf>({
  columns,
  minColumnWidth = 60,
  layoutStorageKey,
  ...rest
}: ProTableProps<RecordType>) {
```

改为：

```tsx
export default function ProTable<RecordType extends RecordTypeOf>({
  columns,
  minColumnWidth = 60,
  layoutStorageKey,
  pagination,
  ...rest
}: ProTableProps<RecordType>) {
```

必须解构出来，否则它留在 `rest` 里会把合并结果覆盖掉。

- [ ] **Step 4: 加 pageSize 状态与持久化**

在同文件「布局变动即自动写回」那个 `useEffect`（原第 280-290 行）之后插入：

```tsx
  // —— 每页条数：与列布局分开的独立存档 ——
  // 分开存的原因：protable:layout 驱动着「还原默认列布局」按钮的显隐与语义，
  // 若把 pageSize 并进去，仅改过条数的用户也会看到该按钮，且点它会连条数一起重置。
  const pageSizeStorageKey = layoutStorageKey
    ? `protable:pageSize:${layoutStorageKey}`
    : undefined;

  const [pageSize, setPageSize] = useState(DEFAULT_PAGE_SIZE);
  const pageSizeRestoredRef = useRef(false);

  // 挂载后一次性恢复（与列布局同理，放 effect 防 SSR hydration 错位）
  useEffect(() => {
    if (!pageSizeStorageKey) return;
    try {
      const raw = window.localStorage.getItem(pageSizeStorageKey);
      const n = raw === null ? NaN : Number(raw);
      // 只接受白名单内的档位；存档被篡改或来自旧版本时一律退回默认
      if (PAGE_SIZE_CHOICES.includes(n)) setPageSize(n);
    } catch {
      // 隐私模式等场景下静默降级
    }
    pageSizeRestoredRef.current = true;
  }, [pageSizeStorageKey]);

  // 变动即写回
  useEffect(() => {
    if (!pageSizeStorageKey || !pageSizeRestoredRef.current) return;
    try {
      window.localStorage.setItem(pageSizeStorageKey, String(pageSize));
    } catch {
      /* 忽略 */
    }
  }, [pageSizeStorageKey, pageSize]);

  /**
   * 合并分页配置：本组件提供默认值，调用方传入的同名字段优先。
   * 调用方显式给了 pageSize 时整体让位（不接管、也不持久化），避免两处状态打架。
   * 不设 current —— 保持非受控，排序后仍停留当前页。
   */
  const mergedPagination = useMemo(() => {
    if (pagination === false) return false;
    const caller =
      pagination && typeof pagination === "object" ? pagination : undefined;
    if (caller?.pageSize !== undefined) return caller;
    return {
      pageSize,
      showSizeChanger: true,
      pageSizeOptions: PAGE_SIZE_CHOICES.map(String),
      onShowSizeChange: (_current: number, size: number) => setPageSize(size),
      ...(caller ?? {}),
    };
  }, [pagination, pageSize]);
```

- [ ] **Step 5: 把合并结果传给 Table**

同文件末尾的 `<Table>`（原第 405-409 行）：

```tsx
    <Table<RecordType>
      {...rest}
      columns={displayColumns}
      components={components}
    />
```

改为：

```tsx
    <Table<RecordType>
      {...rest}
      columns={displayColumns}
      pagination={mergedPagination}
      components={components}
    />
```

- [ ] **Step 6: 类型检查**

Run: `npm run typecheck`
Expected: 无输出（零错误）。

> 此时两个列表页仍在传 `pageSize: 10`，按合并规则「调用方优先」，本组件会整体让位，
> 所以界面上暂时看不到变化——这是预期的中间状态，Task 5 移除后即生效。

- [ ] **Step 7: 提交**

```bash
git add src/components/common/ProTable.tsx
git commit -m "ProTable 接管每页条数：默认 10，可选 10/20/30/50/100

存独立的 localStorage key（protable:pageSize:<key>），不并入列布局存档 ——
后者驱动「还原默认列布局」按钮的显隐与语义，合并会导致仅改过条数的用户
看到该按钮、且点它连条数一起重置。

读档做白名单校验，只接受 5 个档位内的值，篡改存档或旧版本数据一律退回默认。
只接管 pageSize 不接管 current，排序后仍停留当前页。
调用方显式传 pageSize 时整体让位，避免两处状态打架。"
```

---

## Task 5: 两个列表页改用 ProTable 的默认分页

**Files:**
- Modify: `src/app/work-orders/page.tsx`
- Modify: `src/app/requirements/page.tsx`

移除两页自带的 `pageSize` 与 `showSizeChanger`，让 Task 4 的接管生效。

- [ ] **Step 1: 工单列表页**

`src/app/work-orders/page.tsx` 的 ProTable：

```tsx
        pagination={{
          pageSize: 10,
          showSizeChanger: false,
          showTotal: (total) => `共 ${total} 条`,
        }}
```

改为：

```tsx
        pagination={{
          showTotal: (total) => `共 ${total} 条`,
        }}
```

- [ ] **Step 2: 需求列表页**

`src/app/requirements/page.tsx` 的 ProTable，同样：

```tsx
        pagination={{
          pageSize: 10,
          showSizeChanger: false,
          showTotal: (total) => `共 ${total} 条`,
        }}
```

改为：

```tsx
        pagination={{
          showTotal: (total) => `共 ${total} 条`,
        }}
```

- [ ] **Step 3: 确认没有遗漏**

Run: `grep -rn "showSizeChanger" src/`

Expected: 只剩 `src/components/common/ProTable.tsx` 一处（`showSizeChanger: true`）。
若两个列表页仍有命中，说明漏改。

- [ ] **Step 4: 类型检查**

Run: `npm run typecheck`
Expected: 无输出（零错误）。

- [ ] **Step 5: 提交**

```bash
git add src/app/work-orders/page.tsx src/app/requirements/page.tsx
git commit -m "两个列表页改用 ProTable 的统一分页配置

移除各自写死的 pageSize: 10 与 showSizeChanger: false，只保留 showTotal。
分页默认值与持久化由 ProTable 接管，消除两处重复的字面量。"
```

---

## Task 6: 重新构建 + 手动验证核心保险逻辑

**Files:** 无（验证任务）

**为什么必须单独做**：Task 3 的断言只能覆盖回归（正常路径没坏）。**「存量记录的
合法值被删除后仍可保存」这个核心场景无法用纯 HTTP 构造**——API 不允许创建 system/status
非法的记录。这步失败时**不会有任何报错提示**，只会静默地继续 400。

- [ ] **Step 1: 确认服务是最新构建**

Task 3 Step 3 已重建并启动。若中途改过代码，重跑一遍停服 → build → start。

- [ ] **Step 2: 需求侧 —— 植入脏数据**

```bash
cd "G:/Desktop/代码/四川物流营运部记录系统"
sqlite3 prisma/dev.db "SELECT requirementId, system FROM Requirement LIMIT 1;"
```

记下查到的 `requirementId` 与 `system`（后面还原要用）。

```bash
sqlite3 prisma/dev.db "UPDATE Requirement SET system='OMS' WHERE requirementId='<上一步查到的ID>';"
sqlite3 prisma/dev.db "SELECT requirementId, system FROM Requirement WHERE requirementId='<上一步查到的ID>';"
```

Expected: system 显示为 `OMS`。

- [ ] **Step 3: 需求侧 —— 三个方向各测一次**

先拿到内部主键（PUT 用的是 `id` 不是 `requirementId`）：

```bash
curl -s "http://localhost:3000/api/requirements?keyword=<上一步的ID>" | python -c "import sys,json;print(json.load(sys.stdin)[0]['id'])"
```

记下这个 `id`，然后：

```bash
# ① 传「未变更」的 OMS → 期望 200（保险生效）
curl -s -o /dev/null -w "%{http_code}\n" -X PUT "http://localhost:3000/api/requirements/<id>" \
  -H "Content-Type: application/json" -d '{"system":"OMS","remark":"保险测试"}'

# ② 传枚举外的新值 → 期望 400
curl -s -o /dev/null -w "%{http_code}\n" -X PUT "http://localhost:3000/api/requirements/<id>" \
  -H "Content-Type: application/json" -d '{"system":"不存在的系统"}'

# ③ 不传 system，只改备注 → 期望 200
curl -s -o /dev/null -w "%{http_code}\n" -X PUT "http://localhost:3000/api/requirements/<id>" \
  -H "Content-Type: application/json" -d '{"remark":"保险测试2"}'
```

Expected: `200` / `400` / `200`。

**改动前这三条会是 `400` / `400` / `200`** —— 第一条正是本次要修的场景。

- [ ] **Step 4: 需求侧 —— 还原**

```bash
sqlite3 prisma/dev.db "UPDATE Requirement SET system='<Step 2 记下的原值>' WHERE requirementId='<Step 2 的ID>';"
sqlite3 prisma/dev.db "SELECT requirementId, system FROM Requirement WHERE requirementId='<Step 2 的ID>';"
```

Expected: system 已还原。**这步不能漏**，否则库里留一条脏数据。

- [ ] **Step 5: 工单侧 —— 先把表填上数据**

工单表当前是 0 条，先建一条：

```bash
curl -s -X POST "http://localhost:3000/api/work-orders" \
  -H "Content-Type: application/json" \
  -d '{"date":"2026-09-16","title":"保险验证工单","content":"用于验证枚举兼容保险，验证后删除","system":"WMS"}' \
  | python -c "import sys,json;d=json.load(sys.stdin);print(d['id'])"
```

记下返回的 `id`。

- [ ] **Step 6: 工单侧 —— status 三个方向各测一次**

```bash
# 植入：把 status 改成一个已删除的值
sqlite3 prisma/dev.db "UPDATE WorkOrder SET status='已废弃' WHERE id='<Step 5 的id>';"

# ① 传「未变更」的 已废弃 → 期望 200
curl -s -o /dev/null -w "%{http_code}\n" -X PUT "http://localhost:3000/api/work-orders/<id>" \
  -H "Content-Type: application/json" -d '{"status":"已废弃","remark":"保险测试"}'

# ② 传枚举外的新值 → 期望 400
curl -s -o /dev/null -w "%{http_code}\n" -X PUT "http://localhost:3000/api/work-orders/<id>" \
  -H "Content-Type: application/json" -d '{"status":"乱写"}'
```

Expected: `200` / `400`。

- [ ] **Step 7: 工单侧 —— 清理验证数据**

```bash
curl -s -o /dev/null -w "%{http_code}\n" -X DELETE "http://localhost:3000/api/work-orders/<Step 5 的id>"
sqlite3 prisma/dev.db "SELECT COUNT(*) FROM WorkOrder;"
```

Expected: `204`，然后工单数为 `0`（回到验证前的状态）。

- [ ] **Step 8: 确认数据库回到干净状态**

```bash
sqlite3 prisma/dev.db "PRAGMA integrity_check; SELECT requirementId, system FROM Requirement;"
```

Expected: `ok`，且所有需求的 system 都是合法值（WMS / ERP / TMS / SCM / 追溯中心 / 其他）。

---

## Task 7: 文档同步

**Files:**
- Modify: `docs/用户手册.md`
- Modify: `docs/API-接口文档.md`

系统取值在这几处文档里是**硬编码**的，不在单一来源体系内，改数组不会带动它们。

- [ ] **Step 1: 先定位所有出现处**

Run: `grep -rn "OMS" docs/ src/ README.md`

Expected: 命中 `docs/用户手册.md`（约第 51、230、250、320 行）与
`docs/API-接口文档.md`（约第 15 行）。`src/` 下应无命中（Task 1 已清）。

- [ ] **Step 2: 替换 API 文档的枚举表**

`docs/API-接口文档.md` 第 15 行附近的枚举口径表，把 system 一行的取值
`WMS / ERP / OMS / TMS / 其他` 改为 `WMS / ERP / TMS / SCM / 追溯中心 / 其他`。

- [ ] **Step 3: 替换用户手册的三张字段表**

`docs/用户手册.md` 第 51、230、250 行，同样把 `WMS / ERP / OMS / TMS / 其他`
改为 `WMS / ERP / TMS / SCM / 追溯中心 / 其他`。

- [ ] **Step 4: 修正用户手册 7.1 节的示例代码**

第 320 行附近的代码块现在写的是含 `SRM` 的示例，与实际不符。改为反映当前真实取值：

```js
["WMS", "ERP", "TMS", "SCM", "追溯中心", "其他"]
```

- [ ] **Step 5: 修正 API 文档关于「可直接增删」的说法**

`docs/API-接口文档.md` 第 30 行附近写着「`SYSTEM_OPTIONS` 未在设计文档中枚举，
可直接增删」。补充一句说明删选项的后果：

```
未在设计文档中枚举，可直接增删。删除某选项后，库中已使用该值的存量记录仍可正常
编辑（后端对「值未变更」的提交跳过枚举校验），但无法再选回该值。
```

- [ ] **Step 6: 用户手册补充每页条数说明**

在「3.9 刷新数据」之后新增一节：

```markdown
### 3.10 调整每页显示条数

表格右下角有每页条数选择器，默认 10 条，可切换 10 / 20 / 30 / 50 / 100。

- 你选的条数会**记在浏览器里**，下次打开还是这个值；
- **工单和需求两张表各记各的**，互不影响；
- 切换条数后会自动回到第 1 页。

> 注意：排序不会改变你所在的页码。在第 3 页点排序，看到的仍是排序后的第 3 页。
```

- [ ] **Step 7: 用户手册补充删除系统选项的注意事项**

在 7.1 节（新增下拉选项）末尾追加：

```markdown
**删除选项前请先确认没有存量数据。** 例如想删掉「TMS」，先在两张表里筛一下是否
还有系统的值为 TMS 的记录。后端已对「值未变更的提交」跳过枚举校验，所以即使有
存量记录，它们仍能正常编辑；但那些记录无法再选回这个已删除的值，只能改成别的系统。
```

- [ ] **Step 8: 确认没有遗留**

Run: `grep -rn "OMS" docs/ README.md src/`

Expected: 无输出。

- [ ] **Step 9: 提交**

```bash
git add docs/用户手册.md docs/API-接口文档.md
git commit -m "文档同步：系统取值变更 + 每页条数说明

用户手册三张字段表与 API 文档枚举表的 system 取值改为
WMS / ERP / TMS / SCM / 追溯中心 / 其他；修正 7.1 节与实际不符的示例代码。

用户手册新增 3.10 每页条数说明；7.1 节补充删除选项前的注意事项。
API 文档补充「删除选项后存量记录仍可编辑、但无法选回该值」。"
```

---

## Task 8: 用户在浏览器中验证（交付门槛）

**依据：** 项目硬规则第 3 条——**UI 改动必须由用户在浏览器中实际验证后才算完成**，
本环境无浏览器自动化。以下各项**不能由实施者代为勾选**。

**前置**：确认服务已用最新代码构建并启动（Task 3 Step 3-4 或 Task 6 Step 1）。

- [ ] **V1. 系统下拉框内容正确** —— 工单表单、需求表单、工单列表的「按系统筛选」、
      两个列表内联的「系统」格，五处都应显示 **WMS / ERP / TMS / SCM / 追溯中心 / 其他**，
      **没有 OMS**。

- [ ] **V2. 右下角出现条数选择器**，默认显示 10。

- [ ] **V3. 能切换** 20 / 30 / 50 / 100，表格行数随之变化，且切完自动回到第 1 页。

- [ ] **V4. 记住选择** —— 切到 50 后刷新整个页面，应仍是 50。

- [ ] **V5. 两表独立** —— 工单页设成 50 后进需求页，需求页应仍是 10（或它自己记住的值）。

- [ ] **V6. 排序不受影响** —— 双击列名排序后，**仍停留在当前页码**，不该跳回第 1 页。

- [ ] **V7. 系统列排序顺序正确** —— 双击工单列表的「系统」列名，升序应是
      WMS → ERP → TMS → SCM → 追溯中心 → 其他。

- [ ] **V8. 内联编辑与表单保存正常** —— 列表内联改「系统 / 状态」并提交、
      编辑页改完保存，均成功。

- [ ] **V9. 无回归** —— 筛选、内联编辑、提交、还原、删除、转需求均正常。

---

## 完工标准

- `npm run check:sort` 15 项全过
- `npm run typecheck` 零错误
- `npm run build` 通过
- `npm run api:smoke` **112 项**全绿
- Task 6 的手动验证三个方向结果符合预期，且数据已还原/清理
- `grep -rn "OMS" docs/ src/ README.md` 无输出
- Task 8 的 V1-V9 全部由**用户**确认通过

---

## 明确不做（防止范围蔓延）

- 不改 `currentNode` 的校验（取值受设计文档管控，增删属需求变更）
- 不接管分页的 `current`（保持非受控，排序后不跳页）
- 不做后端分页（数据本就是前端全量拉取）
- 不修 `EditableChip` 的 `colorOf` 死代码（仅记录）
- 不把 `SYSTEM_COLORS` 的类型收窄为 `Record<SystemValue, string>`（仅记录）
