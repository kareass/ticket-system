# 依赖
/node_modules

# 构建产物
/.next
/.next-build
/out
/build
tsconfig.tsbuildinfo

# 环境变量（本地配置不入库，模板为 .env.example）
.env
.env.local
.env.*.local

# 数据库（SQLite 开发库）
prisma/*.db
prisma/*.db-journal
*.db
*.db-journal

# 日志与临时文件
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# 系统与编辑器
.DS_Store
Thumbs.db
~$*.xlsx
~$*.docx

# 第三方 skill 拷贝（不作为本项目代码管理）
mattpocock-skills/

# 调度中间产物（总调度下发的任务卡等，不入库）
_dispatch/

# 测试与覆盖率
/coverage
